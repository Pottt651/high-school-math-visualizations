Problems[15]={title:'离散前缀和的唯一最大值',statement:`等比数列首项 ${M.inline('a_1=1/64')}、公比 ${M.inline('q')}，${M.inline(String.raw`b_n=\log_{0.5}a_n`)}，${M.inline('B_n')} 为 ${M.inline('b_n')} 的前 ${M.inline('n')} 项和。当且仅当 ${M.inline('n=4')} 时 ${M.inline('B_n')} 取得最大值，求 ${M.inline('q')} 的范围。选项：A ${M.inline(String.raw`(3,2\sqrt3)`)}；B ${M.inline('(3,4)')}；C ${M.inline(String.raw`(2\sqrt2,4)`)}；D ${M.inline(String.raw`(2\sqrt2,3\sqrt2)`)}。`,mount(host){const C=Lab.C;return YP24.mount(host,{id:'yp24-15',label:'前n项和的离散点，展示唯一与并列最大值',initial:{q:3.2,n:4},controls:'<label>公比 q <input data-k="q" type="range" min="1.8" max="5.5" step=".02"></label><label>n <input data-k="n" type="range" min="1" max="10" step="1"></label><button data-action="low">下端点 2√2</button><button data-action="high">上端点 4</button><button data-action="inside">区间内 q = 3.2</button>',action:(s,a)=>s.q=a==='low'?2*Math.SQRT2:a==='high'?4:3.2,model:s=>YP24.sum(s.q,s.n),
 draw(svg,s){const m=YP24.sum(s.q,s.n),lo=Math.min(-1,...m.values),hi=Math.max(...m.values)+3,p=Lab.plot(svg,{xmin:.4,xmax:10.6,ymin:lo,ymax:hi,equal:false,pad:53});p.axes({stepX:1,x:'n',y:'Bₙ'});for(let i=0;i<10;i++){const k=i+1,color=m.best.includes(k)?C.target:C.blue;p.line([k,0],[k,m.values[i]],{stroke:color,width:1,opacity:.35});p.dot([k,m.values[i]],m.best.includes(k)?`最大：n=${k}`:'',color);}const selected=p.to([s.n,m.B]);p.add(`<circle cx="${selected[0]}" cy="${selected[1]}" r="7" fill="none" stroke="${C.gold}" stroke-width="2"/>`);p.screenText(18,28,'只比较整数位置，不把连续顶点当答案',{size:18});p.finish();},
 read:s=>{const m=YP24.sum(s.q,s.n);return`<p>当前 ${M.inline(`q=${M.number(s.q,5)}`)}，最大值在 ${m.best.map(n=>'n='+n).join('、')} 取得；${m.best.length===1&&m.best[0]===4?'满足唯一最大值要求。':'不满足“当且仅当 n=4”。'}</p><p>${M.inline(String.raw`b_4=${M.number(m.b4,4)},\quad b_5=${M.number(m.b5,4)}`)}</p>${M.answer(`选 C：${M.inline(String.raw`2\sqrt2<q<4`)}，两端都排除。`)}`;},
 proof:`<section class="proof-step"><h3>① 等比取对数，变为等差</h3><p>对数对所有项都有意义，须 ${M.inline('q>0')}。令 ${M.inline(String.raw`c=\log_2q`)}，则</p>${M.block(String.raw`a_n=\frac1{64}q^{n-1}`)}${M.block(String.raw`b_n=6-(n-1)c`)}${M.block(String.raw`B_n=6n-\frac{n(n-1)}2c`)}</section><section class="proof-step"><h3>② 唯一在第4项后达到峰值</h3><p>若 ${M.inline(String.raw`q\le1`)}，则所有 ${M.inline('b_n>0')}，前缀和没有最大值，排除。若 ${M.inline('q>1')}，${M.inline('b_n')} 严格递减。</p><p>因此唯一峰值在 ${M.inline('n=4')}，当且仅当“加第4项仍增加，加第5项开始减少”：</p>${M.block(String.raw`b_4>0,\quad b_5<0`)}</section><section class="proof-step"><h3>③ 解严格不等式并核对边界</h3>${M.block(String.raw`6-3c>0,\quad6-4c<0`)}${M.block(String.raw`\frac32<c<2`)}<div class="target">${M.block(String.raw`2\sqrt2<q<4`)}</div><p>下端点有 ${M.inline('B_4=B_5')}，上端点有 ${M.inline('B_3=B_4')}，都不是唯一最大值。图只展示前10项，后续递减由等差项的符号严格保证。</p></section>`});}};
